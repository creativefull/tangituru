
  <!-- 
  Background display options (use class="CLASS"):
  1. .bg-stretch = stretches background to cover whole area if small or shrinks it if large image
  2. .bg-fixed = fixes background position
  3. .bg-norepeat = stops background repeating
  4. .bg-center = centers the background horizontally
  5. .bg-nofilter = removes the overlay filter
  Options can be combined ie. class="bg-norepeat bg-center" although bg-stretch & bg-fixed will cancel each other out
  -->  
  <header id="header" class="bg-fixed" {{#if @blog.cover}}style="background-image: url({{@blog.cover}})"{{/if}}>
    {{> _mini_menu}}
    
    <!-- .filter class overlays the background image with primary colour -->
    <div class="filter block">
      <div class="container">
        <div class="row">
          <!-- Blog title, description & logo -->
          <!-- to unstyle the logo add class="img-nostyle" -->
          {{#if @blog.logo}}
          <div class="col-md-3 photo"><a href="{{@blog.url}}" itemprop="url"><img src="{{@blog.logo}}" alt="Blog Logo" class="blog-img" itemprop="image" /></a></div>
          {{/if}}
          <div class="{{#if @blog.logo}}col-md-9{{else}}col-md-12{{/if}} details">
            <h1 class="primary-focus" itemprop="name"><a href="http://tangituru.com">{{@blog.title}}</a></h1>
            <h2 class="secondary-focus" itemprop="description">{{@blog.description}}</h2>
            <a href="{{@blog.url}}/rss/" class="btn btn-lg btn-icon-right btn-primary-grad">Subscribe <i class="fa fa-rss"></i></a>
          </div>
        </div>
      </div>
    </div>     
  </header>